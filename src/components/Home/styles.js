import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  root: {
    marginTop: "2rem",
  },
  buttons: {
    display: "flex",
    justifyContent: "space-around",
    maxWidth: "500px",
  },
}));
